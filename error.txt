BÀN GIAO — PHASE 9 (viết 2026-09-14)
Repo: ~/scanner · venv .venv · service systemd tên scanner
Commit đã push: 71be19f "new scanner" trên main (đã rebase lên 7eda193 của Huong)

===============================================================================
1. ĐÃ LÀM XONG
===============================================================================

Bối cảnh: cách chấm điểm cũ (spike: chg>=5% + rvol>=3, universe từ Alpaca
movers/Yahoo screener) không thể thấy hai setup mình muốn. Một mã vừa breakout
khỏi nền tích luỹ 8 tuần thì +2~3% -> nó KHÔNG lọt vào screener, nên nới ngưỡng
trong scorer.py cũng vô ích. Phase 9 đảo chiều luồng: danh sách theo dõi sinh
ra từ tối hôm trước, phiên sau chỉ canh pivot của đúng những mã đó.

File mới (thuần stdlib -> chạy và tự test được cả trên máy dev không có thư viện):

  bars.py        Kho nến ngày ~3 năm trong SQLite (bảng `bars`).
                 Lưu cả c (thô) và ac (adj close): load(adj=True) back-adjust
                 nên chia 1:10 không biến thành "rơi 90%" giả.
                 16 test.

  structure.py   Đo cấu trúc giá từ nến ngày -> bảng `struct` (1 dòng/mã,
                 dựng lại toàn bộ mỗi tối). Chỉ ĐO, không phán xét.
                 20 test.

  setups.py      Hai setup BO/RV -> bảng `candidates`. Tách hai bước:
                   bo_candidate()/rv_candidate() = buổi tối, đọc `struct`
                   trig_bo()/trig_rv()           = trong phiên, đọc quote
                 Mọi ngưỡng nằm trong hai dict BO/RV ở đầu file.
                 26 test.

  backtest.py    Chạy lại BO/RV trên nến ngày quá khứ. Gọi ĐÚNG hàm của bot,
                 có mốc so sánh "vào mù".
                 19 test.

  tests/test_bars.py, test_structure.py, test_setups.py, test_backtest.py

Sửa file cũ:
  prep.py        Thêm --from-bars: dựng bảng `base` từ kho nến, KHÔNG gọi mạng.
                 Đường mạng cũ giữ nguyên và vẫn là mặc định.
                 Sửa luôn một bug âm thầm: SEC_UA sai -> fetch_cik_map() trả {}
                 -> UPSERT cũ xoá sạch cik của CẢ DB. Nay dùng
                 cik=COALESCE(excluded.cik, base.cik) ở cả hai câu UPSERT.
  ci.yml         Thêm 4 dòng selftest không cần thư viện.
  README.md      Mục Phase 9 đầy đủ (9.a -> 9.8), bản đồ file, bảng DB,
                 lệnh debug, cron mới.

Trạng thái kiểm tra trên máy dev: compileall sạch; 4 selftest ok;
16 + 20 + 26 + 19 test ok. Chưa test đường mạng nào (máy dev không chạy được API).

===============================================================================
2. VIỆC KẾ TIẾP — CẦN MÁY CÓ MẠNG, LÀM THEO ĐÚNG THỨ TỰ
===============================================================================

Bước 1. Lấy code
  cd ~/scanner && git pull && source .venv/bin/activate

Bước 2. Kiểm tra chạy được
  python -m pytest -q
  python bars.py && python structure.py && python setups.py && python backtest.py

Bước 3. Dựng kho nến — LẦN ĐẦU, CHẠY TRONG tmux (mất 30-60 phút)
  tmux new -s bars
  python bars.py --sync --full
  python bars.py --info          # bao nhiêu mã, bao nhiêu nến, ngày mới nhất
  (thoát tmux: Ctrl-b rồi d · vào lại: tmux attach -t bars)
  Chạy thử trước cho nhanh nếu muốn: python bars.py --sync --full --limit 200

Bước 4. Đối chiếu trước khi tin — QUAN TRỌNG, ĐỪNG BỎ
  cp state/baseline.db /tmp/copy.db
  rồi so adv20/prev_close giữa `base` hiện tại và bản --from-bars trên /tmp/copy.db.
  Lệch quá vài phần trăm = kho nến thiếu ngày. Mọi thứ dựa trên baseline sẽ sai
  ÂM THẦM, không có gì báo lỗi.
  CHƯA đối chiếu xong thì CHƯA đổi cron.

Bước 5. Dựng struct + candidates
  python structure.py --build       # in luôn danh sách "sắp vượt pivot"
  python structure.py --show NVDA
  python setups.py --build          # in bảng LÝ DO BỊ LOẠI
  python setups.py --show BO --n 40

  Đọc bảng lý do bị loại: "khong co nen tich luy" chiếm gần hết là BÌNH THƯỜNG
  (phần lớn thị trường không ở nền). "con xa pivot" chiếm gần hết = thị trường
  vừa rơi mạnh, và BO đúng ra nên im lặng.

Bước 6. BACKTEST — đây là việc quan trọng nhất, làm trước khi tin ngưỡng nào
  python backtest.py --run --limit 300
  python backtest.py --run --by base_len
  python backtest.py --run --by base_depth
  python backtest.py --run --by rs_pct
  python backtest.py --run --by rvol
  python backtest.py --run --setup RV --rv-no-fund

  Cách đọc, bốn điều:
   - So med10 với dòng `ngau nhien`. Không hơn = setup không có lợi thế gì.
     55% thắng không nghĩa lý gì nếu vào mù cũng thắng 54%.
   - Cột MAE quan trọng ngang med. MAE trung vị -12% thì dù lãi 12% cũng không
     giao dịch được: đã cắt lỗ trước khi nó lãi.
   - Backtest vào lệnh ở GIÁ ĐÓNG CỬA, còn bot alert GIỮA phiên -> thật sẽ xấu hơn.
   - Kho nến chỉ còn mã ĐANG SỐNG (survivorship bias) -> số đẹp hơn thực tế.
     RV lệch nặng nhất, đừng so win% của RV và BO như hai số cùng thang.

  Sau khi đọc: chỉnh dict BO ở đầu setups.py BẰNG SỐ, không bằng cảm giác.
  Sửa một chỗ đó là cả bot lẫn backtest đổi theo (có test khoá việc này).

Bước 7. Chỉ khi bước 4 và 6 đã ổn thì mới đổi cron (thay dòng prep.py cũ)
  CRON_TZ=America/New_York
  0 8 * * 1-5  cd /home/ubuntu/scanner && .venv/bin/python bars.py --sync >> state/prep.log 2>&1 && .venv/bin/python prep.py --from-bars >> state/prep.log 2>&1 && .venv/bin/python structure.py --build >> state/prep.log 2>&1 && .venv/bin/python setups.py --build >> state/prep.log 2>&1

  Đổi được thì bỏ hẳn một lượt tải yfinance: prep.py hiện tải 4 tháng nến cho
  ~5000 mã rồi ném đi, chỉ giữ 3 con số.

===============================================================================
3. CÒN NỢ TRONG PHASE 9 (chưa viết dòng code nào)
===============================================================================

9.3  fundamentals.py + bảng `fund`  — CHẶN RV
     RV hiện dựng được danh sách theo dõi nhưng KHÔNG alert, vì fund_ok=None
     ("chưa biết") không bao giờ được coi là đạt. Đó là cố ý.
     Nguồn: SEC XBRL frames API
       data.sec.gov/api/xbrl/frames/us-gaap/<concept>/USD/CY2025Q2.json
     Một request trả về cả thị trường cho một chỉ tiêu một quý -> phủ 5000 mã
     bằng ~40 request/tuần. SEC_UA đã có trong .env.
     Điểm cơ bản xây quanh SỐNG SÓT + KHÔNG PHA LOÃNG, không quanh P/E:
     tăng trưởng số lượng cổ phiếu YoY (>25%/năm là cờ đỏ, quan trọng hơn mọi
     chỉ số lợi nhuận với small-cap), tiền/tốc độ đốt tiền >= 6 quý, nợ/vốn chủ,
     lịch sử S-1/S-3/424B5/ATM 12 tháng.
     Frames phủ KHÔNG đầy đủ (cùng khái niệm có nhiều tag XBRL) -> mã thiếu số
     liệu phải là "chưa biết" và bị loại khỏi RV.

9.4  Đảo chiều luồng live
     - Nguồn universe thứ ba: chính bảng `candidates`, lấy quote theo lô.
       Dùng Yahoo batch: trễ 15 phút chấp nhận được với setup theo ngày, và
       volume Yahoo là volume HỢP NHẤT.
       KHÔNG dùng Alpaca free cho việc này: bar/snapshot chỉ có IEX (~2% volume
       toàn thị trường) -> RVOL sai hoàn toàn.
     - scorer.py tách thành score_spike / score_breakout / score_reversal.
     - Vòng mới loop_setups 180s + mốc quét chốt ~15:40 ET (mso ~370) trong
       clock.py + báo cáo sau đóng cửa (breakout hôm nay + mã cách pivot < 3%).

9.5  Cửa sổ tin: ghi tin xuống SQLite (bảng `news`), giữ 30 ngày thay vì dict
     cuộn trong RAM mất sạch sau restart. Cửa sổ tra theo setup: spike 4h,
     BO/RV 30 ngày. Tách "tin mới" khỏi "rủi ro pha loãng" (offering trong 90
     ngày trừ điểm bất kể tuổi). Với BO, KHÔNG có tin gì là điểm cộng ngầm —
     nền im lặng chính là dấu hiệu gom hàng.

9.6  Panel riêng từng setup. BO: nền dài bao lâu, pivot ở đâu, trên pivot mấy %.
     RV: rơi bao sâu, điểm cơ bản, runway, lịch sử pha loãng.

9.8  Thêm cột `setup` vào bảng `alerts` và `outcome`, thêm mốc 1/5/10 ngày.
     px15/px60 là thang đo day-trade; đo swing setup bằng nó luôn ra kết luận sai.
     LÀM ĐƯỢC NGAY và nên làm sớm: cột càng có sớm thì bảng outcome càng có mẫu
     để so ba setup SPIKE/BO/RV với nhau. Giống Phase 1: viết một buổi, nhưng
     cần THỜI GIAN mới có số.

Thứ tự khuyên: backtest (bước 6) -> 9.4 -> 9.3 -> 9.8 -> 9.5 -> 9.6.
Engine spike giữ chạy song song, không xoá.

===============================================================================
4. HAI THỨ NHỎ CÒN LẠI
===============================================================================

a) README.md khoảng dòng 671-680: commit 7eda193 (Huong) thêm dòng deploy
   một-dòng nhưng khối ```bash bị hở — dấu đóng nằm SAU đoạn văn giải thích,
   nên đoạn "Dòng python -c import... đáng làm" sẽ hiện ra dưới dạng code trên
   GitHub. Sửa: chuyển dấu đóng lên ngay sau dòng lệnh. Chưa sửa vì là nội dung
   của người khác.

b) Git chưa cấu hình tên/email, commit 71be19f ghi tác giả là
   Pham <tu-anh.pham@allianzinvestments.lu> (Git tự suy từ username + hostname).
     git config --global user.name "..."
     git config --global user.email "..."
   Chỉ áp dụng cho commit sau. Commit đã push thì đừng amend.

===============================================================================
5. CÓ LỖI THÌ GỬI VỀ GÌ
===============================================================================

- Toàn bộ traceback, không cắt.
- Với bước 3: python bars.py --info (số mã, số nến, ngày mới nhất).
- Với bước 5: cả bảng LÝ DO BỊ LOẠI của setups.py --build.
- Với bước 6: cả bảng backtest, gồm dòng `ngau nhien` và hai cột MFE/MAE.
- Log: tail -50 state/prep.log


===============================================================================
6. WEB ĐIỀU KHIỂN — bước 1+2 đã viết (2026-09-14)
===============================================================================

Mục tiêu: điều khiển setup/cảnh báo/tin nhắn từ web, dùng lại app Cloudflare đã
deploy (repo lux-lookthrough, project Pages `the-professional`).

Hình dạng: VM CHỈ GỌI RA, không nhận vào. Không mở cổng Oracle, không cloudflared,
không TLS, không web server nào trên VM.

  VM (python)                          Cloudflare Pages
    push.py  ──── HTTPS ra ────>  functions/api/scanner  ──>  D1 (scanner_kv)
    poll config/commands <─────                 ^
                                     tab Scanner trong app (browser/PWA)

Vì sao push.py chạy NGOÀI process scanner (cron gọi, không phải asyncio):
một cuộc gọi HTTP treo trong vòng lặp sẽ làm dừng cả vòng quét, và thứ bị treo ở
đây là dashboard — thứ ít quan trọng nhất trong hệ thống. Ranh giới đó không được
phá. main.py chỉ ghi một dòng `beat` vào bảng `kv` (không ra mạng); push.py đọc DB
rồi đẩy lên.

Xoá push.py đi thì bot chạy y nguyên. Để trống SCANNER_PUSH_URL/SCANNER_TOKEN thì
push.py không làm gì. Đó là mặc định.

--- ĐÃ VIẾT ---

repo scanner:
  push.py              mới  — payload + gửi, thuần stdlib, DB mở mode=ro
  tests/test_push.py   mới  — 23 test chạy không cần thư viện + 2 test contract
  main.py              sửa  — write_beat() + gọi trong loop_clock (vòng 20s có sẵn)
  .env.example         sửa  — SCANNER_PUSH_URL, SCANNER_TOKEN
  ci.yml               sửa  — thêm `python push.py` vào selftest

repo lux-lookthrough (screener-ts/apps/desktop/):
  functions/api/scanner/[[path]].ts   mới  — endpoint, token riêng, khoá scanner:*
  schema.sql                          sửa  — bảng scanner_kv
  wrangler.toml                       sửa  — ghi chú SCANNER_TOKEN

  --- bước 3 (viết cùng ngày) ---
  src/adapters/scannerClient.ts   mới  — scannerPull(), đọc bằng x-sync-code
  src/tabs/scannerTab.ts          mới  — tab Scanner chỉ đọc
  src/main.ts                     sửa  — TABS + renderTab + nút menu
  index.html                      sửa  — <section id="tab-scanner">
  src/ui/i18n.ts                  sửa  — nav.scanner + nhóm khoá scan.*

Đã kiểm trên máy dev: compileall sạch; python push.py = 25 kiểm tra OK;
tests/test_push.py = 23 ok / 2 skip (2 test cần main.py, tức cần httpx -> chạy ở
job pytest của CI). tsc --strict sạch cho file TS (đã xác minh tsc thật sự kiểm
file, không bị coi tên [[path]].ts là glob).
Bước 3: `npm run typecheck --workspace @screener/desktop` sạch, `npm run build`
ra dist thành công.

CHƯA test đường mạng nào. Máy dev không chạy được API.

Bẫy trên máy dev (Windows + OneDrive): screener-ts/node_modules/@screener/ RỖNG —
npm workspace link chưa được tạo, nên tsc báo ~25 lỗi "Cannot find module
'@screener/core'" ở các file KHÔNG liên quan gì đến việc mình sửa. Không phải lỗi
code. Chữa một lần:
  cd screener-ts && cmd /c "mklink /J node_modules\@screener\core packages\core"
(node_modules không nằm trong git nên đây là việc của từng máy.)

--- THỨ TỰ CHẠY ---

Bước A. Cloudflare (máy nào cũng được, cần npx + đăng nhập wrangler)
  cd screener-ts/apps/desktop
  npx wrangler d1 execute screener-sync --file=./schema.sql --remote
  npx wrangler pages secret put SCANNER_TOKEN --project-name the-professional
      -> tự sinh một chuỗi dài, dán vào, GIỮ LẠI để bước B dùng
  (cd ../.. && npm run build --workspace @screener/core \
             && npm run build --workspace @screener/desktop)
  npx wrangler pages deploy dist --project-name the-professional
      ^ PHẢI deploy từ apps/desktop, không phải từ gốc monorepo — nếu không thì
        toàn bộ functions/ không được ship (wrangler.toml đã ghi rõ chuyện này).

Bước B. VM
  cd ~/scanner && git pull && source .venv/bin/activate
  python -m pytest -q                     # 2 test contract phải chạy, không skip
  python push.py                          # selftest, không ra mạng

  Thêm vào .env:
    SCANNER_PUSH_URL=https://the-professional.pages.dev/api/scanner
    SCANNER_TOKEN=<chuỗi ở bước A>
  (URL không có dấu / ở cuối. Nếu app có domain riêng thì dùng domain đó.)

  python push.py --ping
      Phải ra: HTTP 200 {"ok": true, "role": "writer", "keys": 0, ...}
      HTTP 401 = token lệch giữa .env và Cloudflare secret.
      HTTP 503 = thiếu binding D1, xem lại wrangler.toml.
      HTTP 404 = deploy sai chỗ (deploy từ gốc monorepo, functions không lên).

  python push.py --all --dry              # xem payload, chưa gửi
  python push.py --all                    # gửi thật
  python push.py --get status             # đọc ngược để chắc đã lên

Bước C. cron (thêm 2 dòng, KHÔNG sửa dòng nào đang có)
  CRON_TZ=America/New_York
  * 4-20 * * 1-5  cd /home/ubuntu/scanner && .venv/bin/python push.py --status >> state/push.log 2>&1
  30 8 * * 1-5    cd /home/ubuntu/scanner && .venv/bin/python push.py --all    >> state/push.log 2>&1

  Dòng --status mỗi phút trong khung 04:00-20:59 ET (phủ cả pre/after-market).
  Dòng --all đặt SAU cron setups.py --build của bước 7 mục 2, không đặt trước.

  Ngân sách D1 free: --status mỗi phút = ~1000 lần/ngày, nhưng push.py bỏ qua khi
  payload KHÔNG ĐỔI (so digest, đã bỏ mốc thời gian ra ngoài) nên số write thật
  thấp hơn nhiều. Đừng bao giờ đổi sang đẩy một dòng mỗi mã: 500 candidates x mỗi
  3 phút = ~130k write/ngày, vượt hạn free và tab sẽ đứng im không báo gì.

Bước D. Xem trên web (chỉ cần A và B, không cần chờ C)
  Mở app -> menu -> "Scanner" (cột phải, dưới Hồ sơ Setup).
  Cần đã đặt access code của app (menu -> Sync). Chưa đặt thì tab hiện đúng một
  nút để đặt.
  Không thấy gì -> đọc ô cảnh báo vàng ở đầu tab, nó nói thẳng thiếu cái gì.
  Nút "Tải lại" ở góc trên: đọc lại từ đầu.

--- TAB SCANNER (bước 3) CÓ GÌ ---

Bốn phần, xếp theo thứ tự "sai thì không ai phát hiện ra" -> "số liệu":

  1. Ô CẢNH BÁO (trên cùng, nền vàng). Mỗi dòng ở đây là một trạng thái mà
     scanner IM LẶNG chứ không báo lỗi — đây là lý do tab này tồn tại:
       - chưa từng đẩy status         (push.py chưa vào cron / token sai)
       - status cũ hơn 10 phút        (cron --status đã dừng)
       - candidates cũ hơn 5 ngày     (load_candidates() trả rỗng -> KHÔNG alert
                                       gì cả, không lỗi, không tin nhắn)
       - không có `beat`              (process chưa chạy, hoặc chạy bản cũ)
       - `beat` cũ hơn 120 giây       (vòng quét đã dừng ghi nhịp tim)
       - beat.dry = true              (đang chạy --dry: quét nhưng không gửi)
       - universe_age > 600 giây      (nguồn giá chết, 10 vòng liền không lấy được)
       - halts_err / news_err
       - spool > 0                    (tin xếp hàng, Telegram đang từ chối)
  2. TRẠNG THÁI: session, uptime, số lần quét, lỗi, universe, alert hôm nay, số
     mã đang track, đẩy lần cuối cách đây bao lâu; rồi tuổi ba bảng bars/struct/
     candidates (đỏ khi > 5 ngày).
  3. DANH SÁCH THEO DÕI: một bảng mỗi setup. Cột: Sym Qual Close Pivot To-pivot
     Base Depth Off-high RS ADV20 ATR% Fund. "To pivot" xanh khi <= 0 (đã trên
     pivot), vàng khi <= 2%. Thẻ "120 / 412" nói thật là đang xem bao nhiêu trên
     tổng bao nhiêu. KHÔNG sắp xếp lại theo cột khác: danh sách đã bị cắt theo
     quality, sắp lại theo "gần pivot nhất" sẽ đọc thành "gần nhất thị trường"
     trong khi nó chỉ là gần nhất trong top N.
  4. LÝ DO BỊ LOẠI: mỗi setup một bảng lý do + số mã + thanh tỉ lệ, kèm thẻ
     "qua lọc" và "bị cắt trần". Có ghi chú nhắc bảng này tính theo NGƯỠNG HIỆN
     TẠI, không phải bảng lưu lúc dựng danh sách.
  5. CẢNH BÁO: ngày mới nhất đang giữ. Cột +15m/+60m/Close/MFE/MAE hiện theo
     PHẦN TRĂM so với giá lúc alert, không hiện giá thô — "+1.4%" trả lời được
     câu "alert đó có ăn không", "18.42" thì không.

Bấm vào bất kỳ dòng nào (candidate hoặc alert) mở đúng cửa sổ biểu đồ của app.

Chỉ tải MỘT lần khi mở tab lần đầu (snapshot đổi theo nhịp cron, tải lại mỗi lần
đổi tab chỉ tiêu read của D1). Nút "Tải lại" đọc lại từ đầu (since=0) chứ không
đọc tăng dần: pull tăng dần khi VM chưa đẩy gì mới sẽ trả về rỗng, và cái đó
trông y hệt như cầu nối bị hỏng.

Tab dùng CHUNG access code với sync (x-sync-code, localStorage `sync:code`).
Chưa có code thì tab hiện nút mở cửa sổ Sync. SCANNER_TOKEN của VM KHÔNG bao giờ
xuống browser.

--- CÒN LẠI, CHƯA VIẾT ---

  Bước 4  config.py — gom ~60 ngưỡng đang rải trong main.py/setups.py/scorer.py
          vào một chỗ, có default + khoảng hợp lệ + snapshot cho backtest.
          Không làm bước này thì UI không xoay được ngưỡng.
  Bước 5  trang cài đặt ghi scanner:config; python reload Ở BIÊN VÒNG LẶP;
          ghi cfg_rev vào bảng alerts để sau còn giải thích được alert nào theo
          luật nào.
  Bước 6  scanner:commands + các nút (tạm dừng, tắt tiếng mã, quét lại).
  Bước 7  xem trước tin Telegram: render.py đẩy HTML đã render lên.

  Thứ tự: bước 3 XONG. Việc kế tiếp là quay lại BACKTEST (mục 2 bước 6), KHÔNG
  phải bước 4. Chưa backtest thì chưa biết xoay ngưỡng về đâu, nên một trang cài
  đặt để xoay ngưỡng bằng cảm giác là thứ tệ hơn không có trang nào.

  Ngoài ra một khi tab Scanner đã lên, nó tự làm việc của mình: nó là chỗ đọc
  bảng "lý do bị loại" và tuổi các bảng mà không cần ssh vào VM.

  MỘT ĐIỀU CẦN ĐỒNG BỘ TAY: MAX_AGE_DAYS = 5 trong scannerTab.ts là bản chép của
  setups.MAX_AGE. Browser không đọc được hằng số Python. Sửa MAX_AGE bên setups.py
  thì sửa cả bên đó, không thì tab sẽ báo "im lặng" sai thời điểm.

--- HAI ĐIỀU ĐÃ CÂN NHẮC, GHI LẠI ĐỂ KHÔNG LÀM LẠI ---

a) KHÔNG dùng /api/sync và bảng `kv` có sẵn. Nó có collapse guard từ chối mọi
   write làm giá trị teo hơn 50% (COLLAPSE_MIN_BYTES=200, COLLAPSE_RATIO=0.5), vì
   với portfolio thì teo = client hỏng đang xoá dữ liệu. Với scanner thì ngược
   lại: candidates từ 400 mã xuống 30 mã sau một phiên thị trường rơi là BÌNH
   THƯỜNG, và snapshot cũ không có giá trị gì. Dùng lại thì đúng những ngày thị
   trường xấu tab sẽ hiện số liệu cũ, và hiện IM LẶNG. Nên: bảng scanner_kv
   riêng, không guard, không history/trash.

b) MỘT NGƯỜI GHI CHO MỖI KHOÁ. App ghi scanner:config và scanner:commands; VM ghi
   mọi khoá còn lại. Không bên nào read-modify-write khoá của bên kia, nên không
   thể mất update và không cần lock. Kết quả của command đi về trên khoá riêng
   (scanner:command_results) do VM sở hữu. Endpoint cưỡng chế việc này bằng 403.


1. Bảng D1 — làm trên máy Windows này

/ping có đếm SELECT COUNT(*) FROM scanner_kv, nên nếu bảng chưa có thì ping ra 503 chứ không phải 200. Chạy cho chắc, schema.sql toàn IF NOT EXISTS nên không đụng gì tới dữ liệu portfolio đang có:

cd "C:/Users/PHAMT/OneDrive - Allianz/Documents/Github/lux-lookthrough/screener-ts/apps/desktop"
npx wrangler d1 execute screener-sync --file=./schema.sql --remote

2. Token mới — trên VM

cd ~/scanner
git pull
sed -i '/^SCANNER_TOKEN=/d' .env
TOK=$(openssl rand -hex 32); echo "SCANNER_TOKEN=$TOK" >> .env; echo "$TOK"

sed xoá đúng dòng cũ, >> giữ nguyên TG_TOKEN / ALPACA_SECRET / SEC_UA. Đừng dùng >.

3. Dán token vào Cloudflare — máy Windows

npx wrangler pages secret put SCANNER_TOKEN --project-name the-professional

Dán 64 ký tự, không kèm dấu xuống dòng. Function .trim() header nhận vào nhưng không trim secret nó lưu — một \n lạc vào là 401 vĩnh viễn mà phía VM trông như không có gì sai.

Nếu sau bước này ping vẫn 401, deploy lại (secret của Pages có thể chỉ ăn từ deployment kế tiếp):

cd ../.. && npm run build --workspace @screener/core && npm run build --workspace @screener/desktop
cd apps/desktop && npx wrangler pages deploy dist --project-name the-professional

4. Ping — trên VM

python push.py --ping

Mong đợi HTTP 200 {"ok":true,"role":"writer","keys":0,...}. Đọc kết quả thế này:

- 200 — xong, đi tiếp
- 401 — token lệch, quay lại bước 3
- 503 — bảng chưa có, quay lại bước 1
- 403 — bản push.py chưa được pull về

5. Restart bot

main.py cũng đổi (nó ghi dòng beat), nên git pull không đủ:

sudo systemctl restart scanner
python push.py --all        # đẩy phát đầu tiên, đừng chờ cron

6. Mở tab Scanner trên web

Cần đã nhập access code (tab dùng x-sync-code để đọc). Nếu tab hiện warn "VM chưa đẩy lần nào" thì quay về bước 5.


================================================================================
2026-09-14 — VIỆC PHẢI CHẠY TIẾP
================================================================================

Sáu bước ở trên: XONG. Cầu nối đã sống, ping ra 200, tab đã đọc được snapshot.
Đừng chạy lại chúng.

Tab hiện đang báo: bars/struct/candidates "table missing", và sau khi bấm Tải lại
thì "vòng quét đã ngừng ghi heartbeat 2m". Hai chuyện khác bản chất nhau:

  - "heartbeat 2m" là BUG CỦA TAB, không phải bot chết. Đã sửa, xem việc B.
  - "table missing" là THẬT, và nó chặn cả backtest. Xem việc A.

Commit liên quan:
  scanner        6dd22dd  push.py: User-Agent + giữ body khi không phải JSON
  lux-lookthrough a113bb34 scanner tab: đo tuổi beat theo lúc PUSH

Thứ tự: A trước (nó quyết định mọi thứ sau đó), B lúc nào cũng được, rồi C, D.


--- VIỆC A. Chẩn đoán DB thiếu bảng — TRÊN VM ---

Chạy đúng bốn lệnh này rồi ĐỌC kích thước file trước khi làm gì tiếp:

  ls -la ~/scanner/state/*.db
  sqlite3 ~/scanner/state/baseline.db ".tables"
  systemctl cat scanner --no-pager | grep -iE "workingdirectory|execstart"
  systemctl status scanner --no-pager | head -15

Vì sao bốn lệnh này là đủ: push.py MỞ ĐƯỢC baseline.db (nếu không thì status sẽ
có db_error, không phải "table missing"), và nó thấy `beat` — tức là thấy các bảng
do store.py tạo tự động mỗi lần kết nối. Nhưng không thấy bảng nào của pipeline
dữ liệu. Chỉ có hai cách để chuyện đó xảy ra, và kích thước file phân biệt chúng:

  A1. baseline.db NHỎ (vài chục KB)
      -> kho nền chưa bao giờ được dựng trên VM này. store.py tự tạo file rỗng
         lúc main.py khởi động. Làm việc C.

  A2. baseline.db LỚN (hàng trăm MB)
      -> có kho thật, nhưng nó ở THƯ MỤC KHÁC với chỗ push.py đang đọc.
         Xem WorkingDirectory của systemd. Nếu nó không phải /home/ubuntu/scanner
         thì `git pull` hôm nay cũng chưa tới được con bot đang chạy, và bản
         push.py mới cũng chưa. Sửa đường dẫn cho khớp rồi restart, đừng copy DB.

         Đây là khả năng đáng nghi hơn, vì bot đã gửi alert Telegram từ trước —
         nghĩa là ở đâu đó CÓ một baseline.db đầy dữ liệu.

Cả hai module đều dùng ROOT/state/baseline.db với ROOT = thư mục chứa file .py,
đường dẫn tuyệt đối. Nên hai checkout khác nhau = hai DB khác nhau, im lặng.


--- VIỆC B. Deploy lại web để lấy fix heartbeat — MÁY WINDOWS ---

  cd "C:/Users/PHAMT/OneDrive - Allianz/Documents/Github/lux-lookthrough/screener-ts"
  npm run build --workspace @screener/core
  npm run build --workspace @screener/desktop
  cd apps/desktop
  npx wrangler pages deploy dist --project-name the-professional

Nội dung fix: `beat` chỉ đi tới browser BÊN TRONG một snapshot. Chưa cài cron thì
snapshot đóng băng ở lúc chạy `push.py --all` bằng tay, beat trong đó cứ già dần,
và Date.now() tố một vòng quét đang sống là đã chết. Giờ đo theo lúc snapshot được
đẩy. Hai cảnh báo tách ra sạch:

  stale     = snapshot không tới       -> cron / mạng / push.py
  beatstale = lúc đẩy thì beat đã cũ   -> process main.py

PHẢI deploy từ apps/desktop, không phải từ gốc monorepo.


--- VIỆC C. Dựng kho nền (chỉ khi kết quả là A1) — TRÊN VM ---

  tmux new -s bars                 # BẮT BUỘC: SSH rớt giữa 45 phút là mất cả buổi
  cd ~/scanner && source .venv/bin/activate
  python bars.py --sync --full     # lần đầu, 30-60 phút
                                   # Ctrl-B D để rời tmux, `tmux a -t bars` để vào lại

bars.py tự lấy universe qua prep.fetch_universe(), nên KHÔNG cần chạy prep.py
trước. Xong thì kiểm tra:

  python bars.py --info AAPL

Đủ cho backtest rồi. Muốn tab và bot live có số thì chạy thêm:

  python prep.py
  python structure.py --build
  python setups.py --build
  python push.py --all


--- VIỆC D. Backtest — TRÊN VM ---

backtest.py đã viết xong (Phase 9.7), không phải làm gì mới. Nó chỉ cần kho nền,
không cần bảng struct hay candidates.

  python backtest.py                      # selftest, không cần DB
  python backtest.py --run --limit 300    # thử nhanh trước
  python backtest.py --run
  python backtest.py --run --by base_len
  python backtest.py --run --setup BO --rvol 2.5

CON SỐ ĐÁNG ĐỌC TRƯỚC TIÊN LÀ DÒNG `ngau nhien`. "55% thắng sau 10 phiên" vô
nghĩa nếu bốc mù mã nào cũng thắng 54%. Nếu BO không cách mốc ngẫu nhiên một
khoảng rõ ràng thì định nghĩa nền chưa kiếm được gì, và chỉnh ngưỡng chỉ là chỉnh
cho vừa nhiễu — đúng cái sai mà Phase 9 đang sửa.

Ba thiên lệch phải nhớ khi đọc kết quả (docstring backtest.py đã ghi):
  - Vào lệnh ở GIÁ ĐÓNG CỬA ngày kích hoạt, còn bot alert GIỮA phiên. Thực tế
    thường xấu hơn, vì alert giữa phiên vào giá cao hơn ở ngày đảo chiều.
  - yfinance chỉ còn mã ĐANG SỐNG. Kết quả đẹp hơn thật, và RV bị nặng nhất —
    đừng so win% của RV với BO như hai số cùng thang đo.
  - rs_pct xếp hạng trong phạm vi mã được backtest, không phải cả thị trường.


--- VIỆC E. cron (làm sau cùng, khi A-D đã ra số) ---

Xem "Bước C. cron" ở mục trên. Chưa cài cron thì tab chỉ có snapshot của lần đẩy
tay gần nhất — không sai, chỉ là cũ, và cảnh báo `stale` sẽ nói đúng chuyện đó.

Sau khi tab lên số, việc tiếp theo là backtest (mục 2 bước 6), không phải bước 4 — như error.txt đã ghi: chưa có số backtest thì không có gì để ngắm khi chỉnh ngưỡng.